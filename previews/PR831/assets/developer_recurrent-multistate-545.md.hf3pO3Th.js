import{_ as t,o as a,c as o,an as i}from"./chunks/framework.9quqapag.js";const m=JSON.parse('{"title":"Recurrent / cyclic multi-state transitions (#545)","description":"","frontmatter":{},"headers":[],"relativePath":"developer/recurrent-multistate-545.md","filePath":"developer/recurrent-multistate-545.md","lastUpdated":null}'),r={name:"developer/recurrent-multistate-545.md"};function s(n,e,c,d,l,h){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="Recurrent-/-cyclic-multi-state-transitions-#545" tabindex="-1">Recurrent / cyclic multi-state transitions (#545) <a class="header-anchor" href="#Recurrent-/-cyclic-multi-state-transitions-#545" aria-label="Permalink to &quot;Recurrent / cyclic multi-state transitions (#545) {#Recurrent-/-cyclic-multi-state-transitions-#545}&quot;">​</a></h1><p>Status: implemented. The maintainer approved approach B (renewal over states) as the default with approach A (CTMC) as a memoryless fast path; both are built. This page keeps the design comparison as the rationale and records what shipped.</p><h2 id="The-gap" tabindex="-1">The gap <a class="header-anchor" href="#The-gap" aria-label="Permalink to &quot;The gap {#The-gap}&quot;">​</a></h2><p>The composed object today is a finite, acyclic event tree. Each event happens at most once and times accumulate forward through <code>sequential</code> / <code>parallel</code> / <code>resolve</code> / <code>compete</code>. That cannot represent a recurrent or back-and-forth transition, where a path forms a cycle and takes an unbounded, random number of steps. The motivating epidemiological cases are waning immunity with reinfection (<code>recovered -&gt; susceptible -&gt; infected -&gt; ...</code>) and hospital cycling (<code>admitted -&gt; ward -&gt; ICU -&gt; ward -&gt; ...</code>).</p><p>The existing branching-competing tutorial already names this limit honestly in its &quot;What this is not&quot; box: no cycles, no calendar-time intensities. This proposal scopes how to lift the no-cycles limit.</p><h2 id="Two-approaches" tabindex="-1">Two approaches <a class="header-anchor" href="#Two-approaches" aria-label="Permalink to &quot;Two approaches {#Two-approaches}&quot;">​</a></h2><h3 id="A.-Continuous-time-Markov-chain-generator-matrix" tabindex="-1">A. Continuous-time Markov chain (generator matrix) <a class="header-anchor" href="#A.-Continuous-time-Markov-chain-generator-matrix" aria-label="Permalink to &quot;A. Continuous-time Markov chain (generator matrix) {#A.-Continuous-time-Markov-chain-generator-matrix}&quot;">​</a></h3><p>Represent the system as a CTMC with a transition-intensity matrix <code>Q</code>. State holding times are exponential, the next state is categorical with probabilities <code>q_ij / -q_ii</code>, and the path likelihood is the standard jump-chain-plus-sojourn product (the <code>msm</code> model).</p><p>Strengths. The memoryless structure gives closed-form transition-probability matrices <code>P(t) = exp(Qt)</code>, which makes panel data (state observed at fixed visit times, transition times unknown) tractable by matrix exponential. A compact, familiar object for survival/biostatistics readers.</p><p>Weaknesses. Holding times are forced exponential. Epidemiological dwell times are rarely exponential (incubation, hospital length-of-stay), and the package&#39;s whole reason to exist is non-exponential, censored delays. Recovering non-exponential sojourns needs phase-type expansion (extra latent sub-states via the linear-chain trick), which inflates the state space and sits awkwardly beside the existing leaf-as-distribution idiom. A generator matrix is a parallel engine: it does not reuse <code>compete</code>, <code>sequential</code> or the censoring wrappers, so it duplicates the grammar rather than extending it.</p><h3 id="B.-Renewal-over-states-semi-Markov,-recommended" tabindex="-1">B. Renewal over states (semi-Markov, recommended) <a class="header-anchor" href="#B.-Renewal-over-states-semi-Markov,-recommended" aria-label="Permalink to &quot;B. Renewal over states (semi-Markov, recommended) {#B.-Renewal-over-states-semi-Markov,-recommended}&quot;">​</a></h3><p>Represent the system as a state graph where each non-absorbing state owns a <code>compete(...)</code> node over its outgoing edges (the cause-specific sojourn distributions). A path is an unfolded renewal: from the current state, draw the winning next-state and its dwell time from that state&#39;s competing-risks node, reset the clock on entry to the next state, and repeat until an absorbing state or a horizon. Cycles are allowed because an edge may point back to an already-visited state.</p><p>Strengths. The sojourn on each edge is ANY <code>UnivariateDistribution</code> (tenet 1), so non-exponential, censored, modified delays compose unchanged. Every per-step quantity is the existing verb machinery: the cause-resolved log sub-density <code>log f_j(t) + sum_{k != j} log S_k(t)</code> is exactly what the <code>Compete</code> node already computes, the winning-cause draw is <code>rand_outcome</code>, and the derived split is <code>probs</code>. The clock-reset sojourn is the semi-Markov holding time the multi-state framing tutorial already documents. The likelihood factorises over completed sojourns, so it is AD-safe and fits with the same tooling the package uses elsewhere. The single new idea over today&#39;s grammar is the loop that keeps taking steps; nothing else is new.</p><p>Weaknesses. The semi-Markov jump-chain likelihood needs the transition TIMES (a line list), so panel data (state-at-visit only) needs an extra marginalisation the CTMC gets for free via <code>P(t)</code>. A still-running final sojourn is right-censored and must be scored as a survival term rather than a density (a production item, not a blocker). The CTMC&#39;s <code>exp(Qt)</code> closed forms are unavailable, so panel-likelihood and some analytic quantities need numeric work.</p><h2 id="Recommendation:-B,-the-renewal-over-states-mode" tabindex="-1">Recommendation: B, the renewal-over-states mode <a class="header-anchor" href="#Recommendation:-B,-the-renewal-over-states-mode" aria-label="Permalink to &quot;Recommendation: B, the renewal-over-states mode {#Recommendation:-B,-the-renewal-over-states-mode}&quot;">​</a></h2><p>It extends the existing idiom rather than bolting on a parallel CTMC engine. The semi-Markov sojourn IS the package&#39;s core object (a censored, non-exponential delay), the per-step scoring IS the existing <code>Compete</code> node, and the only addition is unfolding the steps in a loop. This matches north-star tenet 5 (renewal / branching are compositions: the convolution / racing primitive applied recursively over time) and keeps a cyclic model on the same leaf-as-distribution footing as everything else. The CTMC&#39;s one genuine advantage (panel data via <code>exp(Qt)</code>) is a narrower use case for this package&#39;s line-list-first data, and the memoryless restriction it imposes is exactly the restriction the package exists to avoid. A CTMC fast path for the memoryless special case can be added later behind the same state-graph front-end if panel data becomes a priority.</p><h2 id="What-shipped" tabindex="-1">What shipped <a class="header-anchor" href="#What-shipped" aria-label="Permalink to &quot;What shipped {#What-shipped}&quot;">​</a></h2><p>The renewal-over-states default lives in <code>src/composers/recurrent/</code>:</p><ul><li><p><code>recur(...)</code> builds a <code>RecurrentStates</code> model: each state owns a <code>Compete</code> / <code>Resolve</code> / lone-edge transition node, and a back edge gives a cycle. Any <code>UnivariateDistribution</code> (plain, censored or composed) is a valid edge sojourn.</p></li><li><p><code>rand</code> simulates a <code>StatePath</code> of <code>(from, to, dwell)</code> jumps to absorption or a horizon; <code>logpdf</code> scores a path by reusing the <code>Compete</code> cause-resolved sub-density, and adds the survival term of a horizon-censored final sojourn (right-censoring).</p></li><li><p><code>ctmc(...)</code> builds the memoryless <code>CTMCStates</code> fast path: a generator matrix with closed-form <code>transition_probability</code> <code>P(t) = exp(Q t)</code>, scored through the dispatching <code>logpdf</code> front door (a <code>(time, state)</code> panel uses <code>exp(Q t)</code>, a <code>(from, to, dwell)</code> jump chain the exponential-sojourn term). The matrix exponential is AD-safe. An all-exponential <code>recur(...)</code> auto-dispatches to this representation, and <code>ctmc(::RecurrentStates)</code> converts an existing model.</p></li><li><p><code>recurrent_states_model(template, priors)</code> (DynamicPPL extension) samples each state&#39;s edge priors through the shared <code>_params_submodel</code> and rebuilds the model, so a cyclic model fits with <code>@addlogprob! logpdf(m, path)</code> like the rest of the stack.</p></li></ul><p>Tests cover construction / validation, cyclic simulation, scoring, censoring, the CTMC panel likelihood, and a Turing fit that recovers the sojourn scales. The four AD-relevant paths (the reinfection path, a horizon-censored path, the CTMC jump-chain and the CTMC panel <code>exp(Qt)</code>) are in the ADFixtures <code>:recurrent</code> scenario group, so the full per-backend matrix (ForwardDiff, ReverseDiff, Mooncake forward+reverse, Enzyme forward+reverse) exercises them, with the genuine compiled-backend gaps registered broken (Mooncake reverse on the <code>Compete</code>-node path; Enzyme reverse on the <code>Dict</code>-backed rebuild; Enzyme forward on the matrix exponential). The <a href="/previews/PR831/getting-started/tutorials/recurrent-multistate#recurrent-multistate">Recurrent multi-state transitions</a> tutorial works a reinfection cycle end to end.</p><p>The shipped implementation above supersedes the original design proof of concept, so the standalone prototype script has been retired; the renewal-over-states approach it sketched now lives in <code>src/composers/recurrent/</code> with tests, AD fixtures, and the tutorial linked above.</p><h2 id="Deferred-production-scope" tabindex="-1">Deferred production scope <a class="header-anchor" href="#Deferred-production-scope" aria-label="Permalink to &quot;Deferred production scope {#Deferred-production-scope}&quot;">​</a></h2><p>These extend the shipped feature and are not yet implemented:</p><ul><li><p>panel observation for the SEMI-Markov (non-exponential) path: it needs a transition-probability recursion the exponential CTMC gets free via <code>exp(Qt)</code>;</p></li><li><p>calendar-time-varying (piecewise-constant) edge intensities;</p></li><li><p>a richer event-record schema so a cyclic model reads back through <code>event_names</code> / <code>params_table</code> / <code>update(d, chain)</code> the way the acyclic tree does (the per-state priors front-door is in via <code>recurrent_states_model</code>);</p></li><li><p>ODE lowering of these representations (tracked separately in #758).</p></li></ul>',24)])])}const u=t(r,[["render",s]]);export{m as __pageData,u as default};
