import{_ as o,o as r,c as i,an as a}from"./chunks/framework.Dg5EQQZ6.js";const h=JSON.parse('{"title":"Release notes","description":"","frontmatter":{},"headers":[],"relativePath":"release-notes.md","filePath":"release-notes.md","lastUpdated":null}'),t={name:"release-notes.md"};function n(s,e,d,c,l,u){return r(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="Release-notes" tabindex="-1">Release notes <a class="header-anchor" href="#Release-notes" aria-label="Permalink to &quot;Release notes {#Release-notes}&quot;">​</a></h1><p>CensoredDistributions.jl follows a hybrid changelog approach:</p><ul><li><p><strong>GitHub Releases</strong>: All releases (automatically generated by TagBot)</p></li><li><p><strong>NEWS.md</strong>: Major releases requiring detailed context (shown below)</p></li></ul><p>See <a href="https://github.com/EpiAware/CensoredDistributions.jl/releases" target="_blank" rel="noreferrer">GitHub Releases</a> for minor/patch release information.</p><h2 id="Unreleased" tabindex="-1">Unreleased <a class="header-anchor" href="#Unreleased" aria-label="Permalink to &quot;Unreleased {#Unreleased}&quot;">​</a></h2><h3 id="Bug-fixes" tabindex="-1">Bug fixes <a class="header-anchor" href="#Bug-fixes" aria-label="Permalink to &quot;Bug fixes {#Bug-fixes}&quot;">​</a></h3><ul><li><p><code>compose</code> no longer misclassifies a structural <code>NamedTuple</code> whose user-chosen branch keys are <code>:name</code>/<code>:dist</code> and carry distribution vectors (e.g. <code>(name = [d1, d2], dist = [d3, d4])</code>) as a <code>(name, dist)</code> column table. The column-table heuristic now also requires the <code>:dist</code> column to hold distributions and the <code>:name</code> column to hold non-distribution row labels, so such a NamedTuple builds the intended named <code>Parallel</code> of <code>Sequential</code> chains instead of a silently-wrong 2-branch <code>Parallel</code>.</p></li><li><p><code>Convolved</code> <code>mean</code>/<code>var</code>/<code>std</code> docstrings no longer promise a PMF-weighting fallback for components without an analytic moment. That fallback was unreachable dead code (nothing opted in), so it has been removed; the docstrings now state that each component must provide an analytic <code>mean</code>/<code>var</code> or the call errors.</p></li><li><p>Skip the CDF-saturation early-return in the numeric <code>primarycensored_cdf</code> path when the lower bound is at the distribution boundary (<code>lower == minimum(dist)</code>). Evaluating <code>cdf(dist, lower)</code> there is unnecessary (<code>cdf</code> is 0 by construction) and trips degenerate <code>0·(-Inf)</code> reverse-mode rules in <code>Distributions.jl</code> (e.g. <code>LogNormal</code> via <code>log(0)</code>), contaminating the ReverseDiff tape with NaN. Restores ReverseDiff and Mooncake gradient correctness on <code>PrimaryCensored LogNormal+Uniform numerical</code>. Closes <a href="https://github.com/EpiAware/CensoredDistributions.jl/issues/249" target="_blank" rel="noreferrer">#249</a>.</p></li></ul><h3 id="Breaking" tabindex="-1">Breaking <a class="header-anchor" href="#Breaking" aria-label="Permalink to &quot;Breaking {#Breaking}&quot;">​</a></h3><ul><li><code>primary_censored(...; solver)</code> defaults to <code>GaussLegendre(; n = 64)</code> (was <code>QuadGKJL()</code>). The fixed-node solver traces cleanly through every AD backend, where adaptive quadrature does not. Pass <code>solver = QuadGKJL()</code> explicitly to keep adaptive accuracy.</li></ul><h3 id="AD-gradient-infrastructure" tabindex="-1">AD gradient infrastructure <a class="header-anchor" href="#AD-gradient-infrastructure" aria-label="Permalink to &quot;AD gradient infrastructure {#AD-gradient-infrastructure}&quot;">​</a></h3><ul><li><p>New <code>test/ad/</code> sub-environment with <code>DifferentiationInterfaceTest</code>-driven gradient correctness tests for the <code>logpdf</code> of every censored distribution type, plus unit-level tests for the gamma CDF rrule (<code>_grad_p_a_series</code> baseline, <code>Mooncake.TestUtils.test_rule</code>, and defensive guards). Wired into <code>task test-ad</code>, <code>task test</code>, and a dedicated CI job.</p></li><li><p>New <code>test/ADFixtures</code> path package is the single source of truth for the scenario set and the working/broken backend matrix. The test suite, benchmark suite (<code>benchmark/src/ad_gradients.jl</code>), and docs tutorial all add it as a path dep so the three AD surfaces stay in lock-step.</p></li><li><p>Gradient references are computed with ForwardDiff. Adaptive finite-difference baselines (e.g. <code>central_fdm(5, 1)</code>) disagree with every AD backend by ~10% on Weibull analytical scenarios because <code>PrimaryCensored.logpdf</code> internally finite-differences the CDF with a hardcoded <code>h = 1e-8</code>; ForwardDiff&#39;s Dual propagation through that same FD gives the exact derivative the package computes and matches the other backends to ~1e-6.</p></li><li><p>New &quot;Automatic differentiation backends&quot; tutorial at <code>docs/src/getting-started/tutorials/ad-backends.jl</code> renders the full backend × scenario matrix via <code>DIT.benchmark_differentiation</code> with failures left visible.</p></li><li><p>README gains a per-backend support badge row reflecting the measured state (ForwardDiff full; ReverseDiff tape, Mooncake reverse, and Mooncake forward partial; Enzyme forward/reverse broken).</p></li><li><p>Known follow-ups tracked in <a href="https://github.com/EpiAware/CensoredDistributions.jl/issues/217" target="_blank" rel="noreferrer">#217</a> (<code>gamma_inc</code> <code>Dual</code> dispatch gap on the <code>Distributions.cdf(Gamma)</code> path used by <code>IntervalCensored Gamma arbitrary</code>), <a href="https://github.com/EpiAware/CensoredDistributions.jl/issues/225" target="_blank" rel="noreferrer">#225</a> (Enzyme + DIT-Mooncake interaction).</p></li></ul><h2 id="v0.1.0-Initial-Release" tabindex="-1">v0.1.0 - Initial Release <a class="header-anchor" href="#v0.1.0-Initial-Release" aria-label="Permalink to &quot;v0.1.0 - Initial Release {#v0.1.0-Initial-Release}&quot;">​</a></h2><p>CensoredDistributions.jl extends Distributions.jl to support primary event censoring and interval censoring, enabling modelling of scenarios where observation times are subject to various forms of censoring and truncation.</p><h3 id="Core-functionality" tabindex="-1">Core functionality <a class="header-anchor" href="#Core-functionality" aria-label="Permalink to &quot;Core functionality {#Core-functionality}&quot;">​</a></h3><p><strong>Distribution Constructors</strong></p><ul><li><p><code>primary_censored(dist, primary_event)</code>: Creates primary event censored distributions where an initiating event occurs within a time window, then experiences a delay</p></li><li><p><code>interval_censored(dist, interval)</code>: Creates interval censored distributions for continuous values observed only within discrete intervals (regular or arbitrary intervals)</p></li><li><p><code>double_interval_censored(dist; kwargs...)</code>: Combines primary event censoring, optional truncation, and optional secondary interval censoring in an appropriate mathematical sequence</p></li></ul><p><strong>Distribution Types</strong></p><ul><li><p><code>PrimaryCensored{D, P}</code>: Wraps delay distribution <code>D</code> with primary event distribution <code>P</code></p></li><li><p><code>IntervalCensored{D, T}</code>: Wraps continuous distribution <code>D</code> with interval boundaries of type <code>T</code></p></li><li><p><code>DoubleIntervalCensored</code>: Composition of multiple censoring mechanisms</p></li></ul><p><strong>Extensible CDF Method</strong></p><ul><li><code>primarycensored_cdf()</code>: User-extensible method for computing CDFs of primary censored distributions with analytical solutions for common distribution pairs</li></ul><p><strong>Utility Functions</strong></p><ul><li><code>weight(dist, weights)</code>: Creates weighted distributions for efficient likelihood computation</li></ul><h3 id="Distributions.jl-interface" tabindex="-1">Distributions.jl interface <a class="header-anchor" href="#Distributions.jl-interface" aria-label="Permalink to &quot;Distributions.jl interface {#Distributions.jl-interface}&quot;">​</a></h3><p>Partial implementation of Distributions.jl interface including:</p><ul><li><p><code>pdf</code>, <code>logpdf</code>: Probability density functions</p></li><li><p><code>cdf</code>, <code>logcdf</code>: Cumulative distribution functions</p></li><li><p><code>quantile</code>: Inverse CDF (where analytically tractable)</p></li><li><p><code>rand</code>: Random number generation</p></li><li><p><code>minimum</code>, <code>maximum</code>, <code>insupport</code>: Support queries</p></li><li><p><code>mean</code>, <code>var</code>, <code>std</code>: Moments (where analytically tractable)</p></li></ul><h3 id="Migration-from-primarycensored-R-package" tabindex="-1">Migration from primarycensored R package <a class="header-anchor" href="#Migration-from-primarycensored-R-package" aria-label="Permalink to &quot;Migration from primarycensored R package {#Migration-from-primarycensored-R-package}&quot;">​</a></h3><p>CensoredDistributions.jl provides a Julia-native implementation with enhanced functionality compared to the primarycensored R package:</p><p><strong>Function Mapping</strong></p><ul><li><p>R&#39;s <code>dprimarycensored()</code> → Julia&#39;s <code>pdf(primary_censored(...))</code></p></li><li><p>R&#39;s <code>pprimarycensored()</code> → Julia&#39;s <code>cdf(primary_censored(...))</code></p></li><li><p>R&#39;s <code>qprimarycensored()</code> → Julia&#39;s <code>quantile(primary_censored(...))</code></p></li><li><p>R&#39;s <code>rprimarycensored()</code> → Julia&#39;s <code>rand(primary_censored(...))</code></p></li></ul><p><strong>Parameter Differences</strong></p><ul><li><p>Primary event window: Use <code>primary_event = Uniform(0, pwindow)</code> instead of R&#39;s <code>pwindow</code> parameter</p></li><li><p>Solver selection: Configure via <code>solver</code> keyword instead of R&#39;s integration method parameters</p></li><li><p>Truncation: Apply via <code>truncated()</code> from Distributions.jl or use <code>double_interval_censored()</code> convenience function</p></li></ul><h3 id="Contributors" tabindex="-1">Contributors <a class="header-anchor" href="#Contributors" aria-label="Permalink to &quot;Contributors {#Contributors}&quot;">​</a></h3><ul><li><p>Sam Abbott (<a href="https://github.com/seabbs" target="_blank" rel="noreferrer">@seabbs</a>)</p></li><li><p>Damon Bayer (<a href="https://github.com/damonbayer" target="_blank" rel="noreferrer">@damonbayer</a>)</p></li><li><p>Sam Brand (<a href="https://github.com/sambrand" target="_blank" rel="noreferrer">@sambrand</a>)</p></li><li><p>Michael DeWitt (<a href="https://github.com/dewittpe" target="_blank" rel="noreferrer">@dewittpe</a>)</p></li><li><p>Joseph Lemaitre</p></li></ul>',33)])])}const m=o(t,[["render",n]]);export{h as __pageData,m as default};
