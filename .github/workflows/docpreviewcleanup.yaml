name: Doc Preview Cleanup

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  doc-preview-cleanup:
    uses: EpiAware/.github/.github/workflows/docs-preview-cleanup.yml@2c7ce73459772f11dad46ac7f456e35f0d296faf
    secrets: inherit  # pragma: allowlist secret
